# Servidor
PORT=3000

# Base de datos MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=autotools

# JWT — generá un secret con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=
JWT_EXPIRES_IN=8h

# Email (SendGrid)
SENDGRID_API_KEY=

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:5173
